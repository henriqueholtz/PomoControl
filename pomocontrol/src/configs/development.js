import { EnumEnvironment } from '@pomocontrol-enums'

const config = {
    api: {
        backendUrl: 'https://localhost:1000/api/'
    },
    environment: EnumEnvironment.DEBUG
}

export const DesenvConfig = config;